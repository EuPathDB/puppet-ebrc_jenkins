[Unit]
Description=Jenkins - %i - Instance
After=syslog.target

[Service]
ExecStart=/bin/sh -c "/bin/bash /usr/local/bin/jenkins_init.sh %i"
Type=forking
User=<%= @instance_owner %>

[Install]
WantedBy=multi-user.target
